# AI Trading Bot v2 - 기본 설정
# ================================
# 목표: 일 1% 수익률, 손익비 2:1 기준 최적화

# 트레이딩 기본 설정
trading:
  initial_capital: 10000000      # 초기 자본금 (1000만원)
  market: KRX                    # 시장 (KRX, NASDAQ, NYSE)

  # 거래 시간대
  enable_pre_market: true        # 프리장 (08:00~08:50)
  enable_next_market: true       # 넥스트장 (15:30~20:00)

  # 수수료 (한국투자증권 기준, 2024년 세율 적용)
  fees:
    buy_rate: 0.00015            # 매수 수수료 0.015%
    sell_rate: 0.00195           # 매도 수수료 0.015% + 증권거래세 0.18%

  # 슬리피지
  slippage:
    expected_ticks: 1
    max_ticks: 3


# 리스크 관리 설정 (일 1% 목표 최적화)
risk:
  # === 일일 한도 ===
  daily_max_loss_pct: 3.0        # 하루 최대 손실 3% (5%→3% 축소: 일 1% 목표 시 3일치 손실이 상한)
  daily_max_trades: 50           # 일일 최대 거래 50회 (15→50 확대: 오전 기회 손실 방지)

  # === 포지션 관리 (하이브리드 전략: 스윙 중심) ===
  base_position_pct: 15.0        # 기본 포지션 비율 15% (10→15: 스윙 전략으로 확대)
  max_position_pct: 30.0         # 최대 포지션 비율 30% (20→30: 확신 종목 비중 확대)
  max_positions: 8               # 최대 동시 포지션 8개 (10→8: 집중 투자)
  min_cash_reserve_pct: 10.0     # 최소 현금 보유 10% (5→10: 안전마진 확보)
  min_position_value: 500000     # 최소 포지션 금액 (50만원)
  dynamic_max_positions: true    # 자산 규모에 따라 max_positions 동적 조정

  # === 손절/익절 (기본값, ExitManager에서 오버라이드) ===
  default_stop_loss_pct: 3.0     # 기본 손절 3.0% (2.5→3.0: 스윙 변동성 수용)
  default_take_profit_pct: 7.0   # 기본 익절 7.0% (5→7: 스윙 수익 확대)
  trailing_stop_pct: 3.0         # 트레일링 스탑 3.0% (1.5→3.0: 스윙 여유 확보)

  # === 특별 상황 ===
  hot_theme_position_pct: 50.0   # 핫 테마 시 최대 포지션 50% (70→50: 테마 집중 리스크 완화)
  momentum_multiplier: 1.5       # 강한 모멘텀 시 배팅 증가


# 전략 설정
strategies:
  # 모멘텀 브레이크아웃 (주력 전략: 스윙 3~7일)
  momentum_breakout:
    enabled: true  # 주력 활성화
    holding_period_days: 5       # 목표 보유 기간 5일 (스윙)
    breakout_period: 20          # 돌파 기준 기간 (일)
    confirm_candles: 2           # 돌파 확인 캔들 수
    min_breakout_pct: 0.5        # 최소 돌파 비율
    volume_surge_ratio: 2.5      # 거래량 급증 기준

    # 점수 가중치
    weight_price_momentum: 40.0
    weight_volume_momentum: 30.0
    weight_high_proximity: 20.0
    weight_theme: 10.0

    # 청산 조건 (스윙: 넓은 손익 폭)
    stop_loss_pct: 3.0           # 2.5→3.0: 스윙 변동성 수용
    take_profit_pct: 7.0         # 5→7: 스윙 수익 확대
    trailing_stop_pct: 3.0       # 1.5→3.0: 고점 대비 3% 하락 허용

  # 테마 추종 (단기 1~3일)
  theme_chasing:
    enabled: true                # 활성화 (단기 전략)
    holding_period_days: 2       # 목표 보유 기간 2일 (단기)
    min_theme_score: 70
    entry_window_minutes: 30
    stop_loss_pct: 2.5           # 2.0→2.5: 단기 손절 강화
    take_profit_pct: 5.0         # 4→5: 단기 수익 목표
    trailing_stop_pct: 2.0       # 1.0→2.0: 단기 여유 확보

  # 갭상승 추종 (스윙 3~7일)
  gap_and_go:
    enabled: true                # 활성화 (스윙 전략)
    holding_period_days: 5       # 목표 보유 기간 5일 (스윙)
    min_gap_pct: 2.0
    max_gap_pct: 10.0
    entry_delay_minutes: 30
    pullback_pct: 1.0
    min_volume_ratio: 2.0
    stop_loss_pct: 3.0           # 2.0→3.0: 스윙 변동성 수용
    take_profit_pct: 7.0         # 4→7: 스윙 수익 확대
    trailing_stop_pct: 3.0       # 1.5→3.0: 스윙 여유 확보

  # 평균 회귀 (급락 반등, 보조 전략)
  mean_reversion:
    enabled: false  # 보호 모드: 비활성화
    max_rsi: 30.0
    min_decline_pct: -10.0
    min_volume_ratio: 1.5
    stop_loss_pct: 3.0           # 유지: 역추세 특성상 넓은 SL 필요
    take_profit_pct: 5.0
    trailing_stop_pct: 2.0
    position_size_multiplier: 0.5  # 포지션 절반 (역추세 리스크 관리)


# 브로커 설정
broker:
  type: kis                      # kis, alpaca, ib
  kis:
    env: prod                    # prod, dev (모의투자)
    timeout_seconds: 15


# 데이터 설정
data:
  realtime_source: kis_websocket

  storage:
    type: file
    file_path: ./data/prices

  history:
    lookback_days: 60
    cache_enabled: true


# 로깅 설정
logging:
  level: INFO
  file_enabled: true
  file_path: ./logs
  rotation: 1 day
  retention: 7 days


# 알림 설정
notification:
  telegram:
    enabled: true

  alerts:
    on_signal: false
    on_order: false
    on_fill: false
    on_risk_alert: false
    on_daily_summary: true


# 분할 익절 관리자 (3단계 익절 + ATR 기반 동적 손절) - 스윙 최적화
exit_manager:
  enable_partial_exit: true

  # 3단계 익절 시스템 (스윙: 더 넓은 목표)
  first_exit_pct: 3.0             # 1차 익절 +3% (2→3: 스윙 첫 익절)
  first_exit_ratio: 0.20          # 20% 매도 (25→20: 스윙은 천천히)
  second_exit_pct: 5.0            # 2차 익절 +5% (4→5: 스윙 안정 익절)
  second_exit_ratio: 0.40         # 잔여 80%의 40% = 전체 32% 매도
  third_exit_pct: 7.0             # 3차 익절 +7% (6→7: 스윙 목표 수익)
  third_exit_ratio: 0.5           # 잔여 48%의 50% = 전체 24% 매도

  # ATR 기반 동적 손절 (변동성 대응)
  stop_loss_pct: 3.0              # 기본 손절 3.0% (2.5→3.0: 스윙 여유)
  enable_dynamic_stop: true       # ATR 기반 동적 손절 활성화
  atr_multiplier: 2.5             # ATR × 2.5 (2.0→2.5: 스윙 변동성 수용)
  min_stop_pct: 3.0               # 최소 손절폭 3.0% (2.5→3.0: 스윙 하한)
  max_stop_pct: 7.0               # 최대 손절폭 7.0% (5→7: 스윙 상한)

  # 트레일링 스탑 (스윙 전략)
  trailing_stop_pct: 3.0          # 고점 대비 하락률 3.0% (1.5→3.0: 스윙 여유)
  trailing_activate_pct: 3.0      # 트레일링 활성화 3% (2→3: 1차 익절과 동일)

  include_fees: true


# 종목 스크리너
screener:
  scan_interval_minutes: 5        # 스캔 주기 5분 (10→5: 기회 빠른 포착)
  min_volume_ratio: 2.0
  min_change_pct: 1.0
  max_change_pct: 15.0
  min_market_cap: 1000


# 테마 탐지기
theme_detector:
  scan_interval_minutes: 10       # 테마 스캔 10분 (15→10: 테마 초기 포착)
  min_news_count: 3
  min_theme_score: 70.0


# US 시장 오버나이트 시그널
us_market:
  enabled: true
  fetch_time: "07:30"
  cache_ttl_hours: 24


# 자가 진화 엔진
evolution:
  enabled: true
  analysis_days: 7
  min_trades_for_evolution: 5
  min_confidence_to_apply: 0.6
  auto_rollback_threshold: -2.0
  evaluation_period_days: 7
  send_telegram: false


# 코드 자동 진화 (Claude Code CLI)
code_evolution:
  enabled: true                    # 🟢 활성화 (코드 자동 개선)
  schedule_daily: true             # 매일 실행 (true) or 주간 실행 (false)
  schedule_hour: 18                # 실행 시각 (24시간, 18=오후 6시)
  schedule_day: 5                  # 주간 실행 시 요일 (0=월 ~ 6=일, 5=토)
  auto_merge: true                 # 🟢 자동 머지 (true=검증 통과 시 자동 머지+재시작)
  max_changed_files: 10            # 최대 변경 파일 수
  claude_timeout: 300              # Claude CLI 타임아웃 (초)


# 종목 마스터
stock_master:
  enabled: true
  refresh_time: "18:00"              # 일일 갱신 시간
  skip_weekends: true                # 주말 갱신 스킵

  # 추가 설정
  auto_initial_refresh: true         # 빈 테이블 시 자동 갱신
  cache_ttl_hours: 24                # 캐시 TTL (시간)
  alert_on_consecutive_failures: 3   # N일 연속 실패 시 알림


# 스케줄러 설정
scheduler:
  morning_report_time: "08:00"
  evening_report_time: "17:00"
  evolution_time: "20:30"
  nxt_refresh_hour: 6
  news_impact_threshold: 5


# 대시보드 설정
dashboard:
  enabled: true
  host: "0.0.0.0"
  port: 8080


# 워치리스트 (모니터링 종목)
watchlist:
  # 핵심 대형주
  - code: "005930"
    name: "삼성전자"
    sector: "반도체"

  - code: "000660"
    name: "SK하이닉스"
    sector: "반도체"

  - code: "373220"
    name: "LG에너지솔루션"
    sector: "2차전지"

  # 테마주 (필요시 추가)
